---
title: "Depuración y Descriptivos: Registros no Fatales "
output: html_document
date: "2025-07-10"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Dada la depuración inicial, en el siguiente documento se procede a realizar los ajustes necesarios para enfocar los modelos en tipologías de VBG y los análsis descriptivos de interés.

```{r, echo=FALSE, message=FALSE}
# Carga de libreías
source("../../utils/load_data.R")
library(here)
library(hms)
library(dplyr)
library(forcats)
library(lubridate)
library(stringr)
library(stringi)
library(janitor)
library(explore)
library(ggplot2)
library(sf)
library(readr)
library(patchwork)
```

# Depuración final 

```{r, echo=FALSE}
nf_data <- load_data("INMLCF_no_fatales_debugged_2015_2024_v2.csv", type = "interim")
```


```{r, echo=FALSE, message=FALSE,results='hide', warning=FALSE}
# Conversión de variables a factor 
nf_data <- nf_data %>%
  mutate(across(
    .fns = as.factor                     
  ))
```

### Filtros y agrupaciones


Filtros aplicados:

 *-* Tipo de violencia = Violencia sexual,Violencia interpersonal,Violencia intrafamiliar,Violencia sociopolítica.
 
 *-* Pais de la víctima = Colombia.
 
 *-* Agresor = Pareja, Familiar, Ex-pareja, Conocido.

Agrupaciones:

 *-* Ciclo vital: Adultez (35 - 59 años), Jóvenes (20 - 34 años), Niñas y adolescentes (00 - 19 años) .
 
 *-* Escenario del hecho: Valores distintos de vivienda, se renombran como **espacios sociales**.
 
 *_* Escolaridad: Los niveles de: Doctorado o equivalente,educación técnica profesional y tecnológica, especialización, maestría o equivalente, universitario, educación media o secundaria alta quedan como **Educación superior**.
 
 *-* Rango de hora: De 6 am hasta 5pm como horario laboral, en otro caso queda como fuera del horario laboral.

### Filtros 
```{r, echo=FALSE}
# Filtros 
nf_data <- nf_data %>% filter(def_naturaleza %in% c("Violencia sexual","Violencia interpersonal","Violencia intrafamiliar","Violencia sociopolítica")) 

nf_data <- nf_data %>% filter(!circunstancia_del_hecho %in% c("Bala perdida", "Riña")) # No correspondiente a VBG

nf_data <- nf_data %>% filter(pais_de_nacimiento_de_la_victima =="Colombia") 
```


```{r, echo=FALSE, results='hide'}
# Recodificación del vínculo del agresor
nf_data <- nf_data %>% mutate(
  agresor_group = case_when(
  agresor_ag == "Delincuencia común" ~ "Actores del conflicto",
  agresor_ag == "Miembro de grupos alzados al margen de la ley" ~ "Actores del conflicto",
  agresor_ag == "Miembro de un grupo de la delincuencia organizada" ~  "Actores del conflicto",
    agresor_ag == "Miembros de las fuerzas armadas, de policía, policía judicial y servicios de inteligencia" ~ "Actores del conflicto",
  agresor_ag == "Encargado del cuidado" ~ "Conocido",
  agresor_ag == "Amigo(a)" ~ "Conocido",
  TRUE ~ agresor_ag
))

```

```{r, echo=FALSE, warning=FALSE}
# Aplicando el filtro de que el agresor sólo sea pareja, familiar, ex-pareja, conocido.
nf_data <- nf_data %>% filter(agresor_group %in% c("Pareja","Familiar","Ex-pareja","Conocido", "Actores del conflicto")) 
```

```{r}
# Recodificación de escolaridad
nf_data <- nf_data %>% mutate(
  escolaridad = case_when(
    escolaridad == "Educación básica primaria" ~ "Educación básica primaria y secundaria",
    escolaridad == "Educación básica secundaria o secundaria baja" ~ "Educación básica primaria y secundaria",
    escolaridad == "Doctorado o equivalente" ~ "Educación superior",
    escolaridad == "Educación técnica profesional y tecnológica" ~ "Educación superior",
    escolaridad == "Especialización, maestría o equivalente" ~ "Educación superior",
    escolaridad == "Universitario" ~ "Educación superior",
    escolaridad == "Educación media o secundaria alta" ~ "Educación superior",
    TRUE ~ escolaridad
  )
)
```

```{r, echo=FALSE}
# Recodificación del escenario del hecho
nf_data <- nf_data %>% mutate(
  escenario_del_hecho = case_when(
    escenario_del_hecho != "vivienda" ~ "Espacios sociales",
    TRUE ~ escenario_del_hecho
  )
)
```


```{r, echo=FALSE}
# Recodificación del grupo de edad de la víctima
nf_data <- nf_data %>% mutate(
  ciclo_vital_2 = case_when(
    grupo_de_edad_de_la_victima %in% c("00-04", "05-09") ~ "00-09",
    grupo_de_edad_de_la_victima %in% c("10-14", "15-17", "18-19") ~ "10-19",
    grupo_de_edad_de_la_victima %in% c("20-24", "25-29") ~ "20-29",
    grupo_de_edad_de_la_victima %in% c("30-34", "35-39") ~ "30-39",
    grupo_de_edad_de_la_victima %in% c("40-44", "45-49") ~ "40-49",
    grupo_de_edad_de_la_victima %in% c("50-54", "55-59") ~ "50-59",
    TRUE ~ grupo_de_edad_de_la_victima
  )
)
```

```{r, echo=FALSE}
nf_data <- nf_data %>% mutate(
  ciclo_vital_ = case_when(
    grupo_de_edad_de_la_victima %in% c("00-04", "05-09","10-14","15-17","18-19") ~ "Niñas y adolescentes",
    grupo_de_edad_de_la_victima %in% c("20-24", "25-29","30-34") ~ "Jovenes",
    grupo_de_edad_de_la_victima %in% c("35-39", "40-44","45-49","50-54","55-59") ~ "Adultez",
    TRUE ~ "Adulto mayor"
  )
)
```

```{r}
# Se eliminan los registros de Adultos mayores
nf_data <- nf_data %>% filter(ciclo_vital_ %in% c("Niñas y adolescentes", "Jovenes", "Adultez"))
```

```{r}
# Recodificación de la hora del hecho
nf_data <- nf_data %>% mutate(
  rango_de_hora_del_hecho_x_3_horas = case_when(
    rango_de_hora_del_hecho_x_3_horas == "Sin información" ~ "Sin información",
    rango_de_hora_del_hecho_x_3_horas %in% c("00:00 - 02:59", "03:00 - 05:59") ~ "Madrugada",
    rango_de_hora_del_hecho_x_3_horas %in% c("06:00 - 08:59", "09:00 - 11:59", "12:00 - 14:59", "15:00 - 17:59") ~ "Dia",
    TRUE ~ "Noche"
  )
)
```

```{r}
# Recodificación del día del hecho
nf_data <- nf_data %>% mutate(
  dia_del_hecho = case_when(
    dia_del_hecho %in% c("Sábado", "Domingo") ~ "Fin de semana",
    TRUE ~ "Semana"
  )
)
```


# Gráficos y descriptivos

### Circunstancia del hecho 


```{r, echo=FALSE, results='hide'}
nf_data %>%
  count(def_naturaleza) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = def_naturaleza, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  # Color de las barras
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  # Morado oscuro
            fontface = "bold",
            size = 3.4) +  # Texto en negrita
  labs(title = "Casos de violencia por tipo", x = "", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```


```{r, echo=FALSE}
# # Circunstancia del hecho asociado a violencia interpersonal
# nf_data %>% filter(def_naturaleza == "Violencia interpersonal") %>% group_by(circunstancia_del_hecho) %>% summarise(n=n())
```

### Temporales

```{r, echo=FALSE}
nf_data$ano_del_hecho <- nf_data$ano_del_hecho %>% as.factor()
```

```{r, echo=FALSE}
anio <- nf_data %>%
  count(ano_del_hecho) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = ano_del_hecho, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por año", x = "Año", y = "Frecuencia") +
  theme_minimal()
anio
```

```{r, echo=FALSE}
meses_orden <- c("Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio",
                 "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre")
mes <- nf_data %>%
  count(mes_del_hecho) %>%
  mutate(
    mes_del_hecho = factor(mes_del_hecho, levels = meses_orden),
    proporcion = n / sum(n) * 100
  ) %>%
  ggplot(aes(x = mes_del_hecho, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",
            fontface = "bold",
            size = 3) +
  labs(title = "Casos de violencia por mes", x = "Mes", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

mes
```

```{r, echo=FALSE}
dia <- nf_data %>%
  count(dia_del_hecho) %>%
  mutate(
    proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = dia_del_hecho, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) + 
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082", 
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por día", x = "Día", y = "Frecuencia") +
  theme_minimal()
dia
```


```{r, echo=FALSE}
hora <- nf_data %>%
  count(rango_de_hora_del_hecho_x_3_horas) %>%
  mutate(rango_de_hora_del_hecho_x_3_horas = factor(rango_de_hora_del_hecho_x_3_horas, levels = c("Madrugada", "Dia", "Noche", "Sin información")),
         proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = rango_de_hora_del_hecho_x_3_horas, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por hora", x = "Hora", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1)) 
hora
```

```{r, fig.width=11, fig.height=6.5, echo=FALSE}
(anio | mes ) / (dia | hora)
(dia | hora)
# ggsave("../../reports/bayes/temp_inmlcf.pdf", (anio | mes ) / (dia | hora), width = 14, height = 7)
```

###  Ciclo vital de la víctima
```{r, echo=FALSE}
grupo <- nf_data %>%
  count(grupo_de_edad_de_la_victima) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = grupo_de_edad_de_la_victima, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por grupo de edad", x = "Edad", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))  
grupo
```

```{r}
# unique(nf_data$ciclo_vital_)
orden <- c("Niñas y adolescentes", "Jovenes", "Adultez")
ciclo <- nf_data %>%
  count(ciclo_vital_) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
   mutate(
    ciclo_vital = factor(ciclo_vital_, levels = orden)) %>% 
  ggplot(aes(x = ciclo_vital, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por ciclo vital", x = "", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
ciclo
```



```{r,fig.width = 9, fig.height =4, echo=FALSE}
(grupo | ciclo)
# ggsave(filename = "../../reports/bayes/edad.pdf",
#        plot = (grupo | ciclo), width = 9, height = 4)
```
### Estado civil y escolaridad

```{r}
unique(nf_data$escolaridad)
```

```{r, echo=FALSE}
orden <- c("Sin escolaridad", "Educación inicial y educación preescolar","Educación básica primaria y secundaria","Educación superior","Sin información")
esc <- nf_data %>%
  count(escolaridad) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  mutate(
    escolaridad = factor(escolaridad, levels = orden)) %>% 
  ggplot(aes(x = escolaridad, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3) + 
  labs(title = "Casos de violencia por escolaridad", x = "", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1)) 
esc
```

```{r, echo=FALSE}
estado <- nf_data %>%
  count(estado_civil) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = estado_civil, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082", 
            fontface = "bold",
            size = 3) +  
  labs(title = "Casos de violencia por estado civil", x = "", y = "Frecuencia") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
estado
```
 Los casos de no aplica son para menores de edad.

```{r,fig.width = 9, fig.height =5, echo=FALSE}
(estado | esc)
# ggsave(filename = "../../reports/bayes/escolaridad.pdf",
#        plot = (estado | esc), width = 9, height = 5)
```

### Agresor

```{r, echo=FALSE, results='markup'}
nf_data %>%
  count(agresor_group) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = agresor_group, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3.4) + 
  labs(title = "Casos de violencia por presunto agresor", x = "", y = "Frecuencia") +
  theme_minimal()
```


### Escenario del hecho

```{r, echo=FALSE}
nf_data %>%
  count(escenario_del_hecho) %>%
  mutate(proporcion = n / sum(n) * 100) %>%
  ggplot(aes(x = escenario_del_hecho, y = n)) +
  geom_col(fill = "#AE86D3", alpha = 0.8) +  
  geom_text(aes(label = paste0(round(proporcion, 1), "%")), 
            vjust = -0.2, 
            color = "#4B0082",  
            fontface = "bold",
            size = 3.4) +  
  labs(title = "Casos de violencia por escenario_del_hecho", x = "", y = "Frecuencia") +
  theme_minimal()
```

## Variables de interés vs Tipo de violencia

```{r}
nf_data$ciclo_vital_ <- nf_data$ciclo_vital_ %>% as.factor()
```


```{r, echo=FALSE}
# Calcular proporciones por ciclo_vital y def_naturaleza
orden <- c("Niñas y adolescentes", "Jovenes", "Adultez")
a <- nf_data %>%
  count(ciclo_vital_, def_naturaleza) %>%
  group_by(def_naturaleza) %>%
  mutate(proporcion = round(n / sum(n) * 100, 1)) %>%
  ungroup() %>%
  mutate(
    ciclo_vital = factor(ciclo_vital_, levels = orden)) %>% 
  ggplot(aes(x = proporcion, y = ciclo_vital, fill = def_naturaleza)) +
  geom_col(position = "dodge") +
  geom_text(aes(label = proporcion),
            position = position_dodge(width = 1),
            hjust = 0.5, 
            size = 2.5) +
  scale_fill_manual(values = c(
    "Violencia interpersonal" = "#fc9272",
    "Violencia intrafamiliar" = "#78c679",
    "Violencia sexual" = "#1f9bcf",
    "Violencia sociopolítica" = "#c994c7"
  )) +
  labs(x = "%", y = NULL, fill = "Tipo de violencia", title = "T") +
  theme_minimal() +
  theme(legend.position = "right",
    plot.title = element_text(hjust = 0.5)
  ) +
  ggtitle("Ciclo vital") +
  coord_flip()
a
```

```{r,fig.width = 10, fig.height =4.5, , echo=FALSE}
(ciclo | a)
# ggsave(filename = "../../reports/bayes/ciclo_mod.pdf",
#        plot = (ciclo | a), width = 10, height = 4.5)
```


```{r, echo=FALSE}
# c <- nf_data %>%
#   count(escolaridad, def_naturaleza) %>%
#   group_by(def_naturaleza)%>%
#   mutate(proporcion = round(n / sum(n) * 100, 1)) %>%
#   ungroup() %>%
#   ggplot(aes(x = proporcion, y = escolaridad, fill = def_naturaleza)) +
#   geom_col(position = "dodge") +
#   geom_text(aes(label = proporcion),
#             position = position_dodge(width = 0.9),
#             hjust = -0.2, size = 3) +
#   scale_fill_manual(values = c(
#     "Violencia interpersonal" = "#fc9272",
#     "Violencia intrafamiliar" = "#78c679",
#     "Violencia sexual" = "#1f9bcf",
#     "Violencia sociopolítica" = "#c994c7"
#   )) +
#   labs(x = "%", y = NULL, fill = "def_naturaleza") +
#   theme_minimal() +
#   theme(
#     legend.position = "right",
#     plot.title = element_text(hjust = 0.5)
#   ) +
#   ggtitle("Escolaridad") +
#   coord_flip()+
#   theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

```{r, echo=FALSE}
# d <- nf_data %>%
#   count(escolaridad, def_naturaleza) %>%
#   group_by(def_naturaleza)%>%
#   mutate(proporcion = round(n / sum(n) * 100, 1)) %>%
#   ungroup() %>%
#   ggplot(aes(x = proporcion, y = escolaridad, fill = def_naturaleza)) +
#   geom_col(position = "dodge") +
#   geom_text(aes(label = proporcion),
#             position = position_dodge(width = 0.9),
#             hjust = -0.2, size = 3) +
#   scale_fill_manual(values = c(
#     "Violencia interpersonal" = "#fc9272",
#     "Violencia intrafamiliar" = "#78c679",
#     "Violencia sexual" = "#1f9bcf",
#     "Violencia sociopolítica" = "#c994c7"
#   )) +
#   labs(x = "%", y = NULL, fill = "def_naturaleza") +
#   theme_minimal() +
#   theme(
#     legend.position = "right",
#     plot.title = element_text(hjust = 0.5)
#   ) +
#   ggtitle("Escolaridad") +
#   coord_flip()+
#   theme(axis.text.x = element_text(angle = 45, hjust = 1))
```



```{r}
semana_2 <- nf_data %>%
  count(dia_del_hecho, def_naturaleza) %>%
  group_by(def_naturaleza)%>%
  mutate(
    proporcion = round(n / sum(n) * 100, 1)) %>%
  ungroup() %>%
  ggplot(aes(x = proporcion, y = dia_del_hecho, fill = def_naturaleza)) +
  geom_col(position = "dodge") +
  geom_text(aes(label = proporcion),
            position = position_dodge(width = 1),
            hjust = 0.2, size = 3) +
  scale_fill_manual(values = c(
    "Violencia interpersonal" = "#fc9272",
    "Violencia intrafamiliar" = "#78c679",
    "Violencia sexual" = "#1f9bcf",
    "Violencia sociopolítica" = "#c994c7"
  )) +
  labs(x = "%", y = NULL, fill = "Tipo violencia") +
  theme_minimal() +
  theme(
    legend.position = "right",
    plot.title = element_text(hjust = 0.5)
  ) +
  ggtitle("Semana del hecho") +
  coord_flip()

semana_2
```

```{r,fig.width = 10, fig.height =4.5, , echo=FALSE}
(dia | hora)
# ggsave(filename = "../../reports/bayes/temp_2.pdf",
#        plot = (dia | hora), width = 10, height = 4.5)
semana_2 
# ggsave(filename = "../../reports/bayes/temp_vs_def.pdf",
#        plot = (semana_2 | hora_2), width = 19, height = 4.5)
```

```{r, echo=FALSE}
nf_data %>%
  count(escenario_del_hecho, def_naturaleza) %>%
  group_by(def_naturaleza)%>%
  mutate(proporcion = round(n / sum(n) * 100, 1)) %>%
  ungroup() %>%
  ggplot(aes(x = proporcion, y = escenario_del_hecho, fill = def_naturaleza)) +
  geom_col(position = "dodge") +
  geom_text(aes(label = proporcion),
            position = position_dodge(width = 0.9),
            hjust = -0.2, size = 3) +
  scale_fill_manual(values = c(
    "Violencia interpersonal" = "#fc9272",
    "Violencia intrafamiliar" = "#78c679",
    "Violencia sexual" = "#1f9bcf",
    "Violencia sociopolítica" = "#c994c7"
  )) +
  labs(x = "%", y = NULL, fill = "def_naturaleza") +
  theme_minimal() +
  theme(
    legend.position = "right",
    plot.title = element_text(hjust = 0.5)
  ) +
  ggtitle("Escenario del hecho") +
  coord_flip()+
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

```{r}
# Selección de variables de interés 
nf_data <- nf_data %>% dplyr::select(def_naturaleza, ciclo_vital_, escolaridad, dia_del_hecho, rango_de_hora_del_hecho_x_3_horas, escenario_del_hecho, zona_del_hecho, agresor_group)
```

```{r}
# save_data(nf_data, "data_nf_modelo")
```

