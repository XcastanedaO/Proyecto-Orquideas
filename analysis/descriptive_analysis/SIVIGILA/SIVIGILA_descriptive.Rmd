---
title: "Análisis Descriptivo información proporcionada por SIVIGILA del 2013 al 2024."
date: "2025-07-02"
output:
  pdf_document:
    toc: true
    number_sections: false
knitr:
  opts_chunk:
    echo: false
editor_options:
  markdown:
    wrap: 72
always_allow_html: true
header-includes:
  - \usepackage{float}
---

```{r, include=FALSE}
knitr::opts_chunk$set(echo=FALSE)
library(dplyr)
library(ggplot2)
source("../../../utils/load_data.R")
source("../../../utils/descriptive_functions.R")
library(here)
library(hms)
library(forcats)
library(lubridate)
library(stringr)
library(tidyr)
library(stringi)
library(knitr)
library(readr)
library(sf)
library(gmodels)   
library(DescTools)
library(kableExtra)
library(broom)
library(purrr)
library(DescTools)
library(dplyr)
library(skimr)
library(scales)
```

\newpage


# Lectura de la base de datos y adaptación de la base de datos

```{r, echo=FALSE, warning=FALSE,message=FALSE}
base_datos <- readRDS(paste0(here(),"/data/interim/SIVIGILA_debugged_v2.rds"))
```

La base de datos utilizada para este análisis descriptivo es el resultado de juntar y depurar las bases de datos proporcionadas por SIVIGILA, la cual recopila información sobre los casos sospechosos de violencia de género que fueron reportados. Así, la base depurada tiene un total `r nrow(base_datos)` de filas y se seleccionaron `r ncol(base_datos)` columnas de interés, las cuales son:

```{r}
colnames(base_datos)
```

Para conocer más sobre las variables se cuenta con un diccionario de datos que enviaron junto a las bases [(Ver aquí)](https://www.ins.gov.co/BibliotecaDigital/diccionario-datos-basicos-.pdf).Adicional, se cuenta con algunas de las fichas de notificación al SIVIGILA que contienen indicaciones sobre muchas variables, la más actualizada es la ficha de 2024 [(Ver   aquí)](https://www.ins.gov.co/buscador-eventos/Lineamientos/875_Violencia_Genero_2024.pdf).

**IMPORTANTE:** Como se indicó anteriormente, este análisis es incluyendo los valores faltantes.

```{r}
# Conversión tipo de dato
base_datos <- base_datos %>% mutate(edad_ = as.numeric(as.character(edad_)))

base_datos <- recodificar_variables(base_datos)
```

# Esquema general de la base de datos

```{r}
skim(base_datos %>% select(c(orient_sex,tip_ss,per_etn,nom_grupo,mujer_cabf,antec,consum_spa,pais_residencia,
departamento_residencia,dist_esp_r,def_naturaleza,departamento_ocurrencia,dist_esp_o,area,zona_conf,escenario,ambito_lug,sust_vict,sexo_agre,conv_agre,departamento_notificacion,dist_esp_n,pac_hos,con_fin,cbmte,ac_mental,remit_prot,inf_aut,evi_mlegal)))
```

# Análisis descriptivo

## Análisis univariado

### Análisis edad

```{r}
# Estadísticos básicos edad
summary(as.numeric(as.character(base_datos$edad_)))
```

```{r}
base_datos %>%
  mutate(edad_ = as.numeric(as.character(edad_))) %>%
  ggplot(aes(x = edad_)) +
  geom_histogram(binwidth = 1, fill = "#AE86D3", color = "white", alpha = 0.85) +  
  geom_vline(xintercept = 14, color = "#4B0082", linetype = "dashed", linewidth = 1) +
  geom_vline(xintercept = 50, color = "#4B0082", linetype = "dashed", linewidth = 1) +
  scale_x_continuous(breaks = seq(0, 130, by = 5), limits = c(0, 131)) +
  labs(
    title = "Distribución de edad en años",
    x = "Edad (años)",
    y = "Número de casos"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(face = "bold",hjust = 0.5),
    axis.text = element_text(color = "black")
  )
```

```{r}
tabla_frec <- base_datos %>%
  mutate(rango = case_when(
    edad_ < 14 ~ "<14",
    edad_ >= 14 & edad_ <= 50 ~ "14-50",
    edad_ > 50 ~ ">50"
  ),
  # Convertir a factor ordenado
  rango = factor(rango, levels = c("<14", "14-50", ">50"))) %>%
  count(rango) %>%
  mutate(freq_rel = n / sum(n),
         freq_acum = cumsum(freq_rel))

# Mostrar en kable
kable(tabla_frec, digits = 3,
      col.names = c("Edad(años)", "Casos", "F.relativa", "F.acumulada"))
```

```{r}
# Orden lógico de los grupos
base_datos <- base_datos %>%
  mutate(grupo_edad = factor(grupo_edad,
                            levels = c("0-5", "6-11", "12-17",
                                       "18-28", "29-59",
                                       "60-84", "85_mas")))
# Resumir conteos
df_heatmap <- base_datos %>%  mutate(ano_hecho = year(fec_hecho)) %>% 
  count(ano_hecho, grupo_edad)

# Gráfico
ggplot(df_heatmap, aes(x = ano_hecho, y = grupo_edad, fill = n)) +
  geom_tile(color = "white") +
  geom_text(aes(label = n), color = "black", size = 3) +
  scale_fill_viridis_c(option = "viridis", direction = -1) +
  labs(title = "Mapa de calor: número de casos por año y grupo etario",
       x = "Año", y = "Grupo de edad", fill = "Casos") +
  theme_minimal()  +
  theme(
    plot.title = element_text(face = "bold",hjust = 0.5),
    axis.text = element_text(color = "black")
  )

```
### Fechas
```{r}
summary(base_datos[c("fec_hecho", "fec_con", "fec_not")])
```
```{r}
base_datos %>%
  pivot_longer(all_of(c("fec_hecho", "fec_con", "fec_not")),
               names_to = "tipo_fecha",
               values_to = "fecha") %>%
  mutate(anio = year(fecha),
         tipo_fecha = recode(tipo_fecha,
                             "fec_hecho" = "Fecha hecho",
                             "fec_con" = "Fecha consulta",
                             "fec_not" = "Fecha notificación")) %>%
  count(tipo_fecha, anio) %>%
  ggplot(aes(x = anio, y = n, group = tipo_fecha)) +
  geom_line(color = "#4B0082", size = 1.2) +
  geom_point(color = "#4B0082", size = 2.2) +
  geom_label(aes(label = n),color = "#4B0082", size = 2.2) +
  facet_wrap(~ tipo_fecha, ncol = 1, scales = "free_y") +
  labs(x = "Año", y = "Número de casos") +
  scale_x_continuous(breaks = function(x) seq(floor(min(x, na.rm = TRUE)),
                                              ceiling(max(x, na.rm = TRUE)), 1)) +
  theme_minimal() +
  theme(
    strip.text = element_text(face = "bold"),
    panel.grid.minor = element_blank()
  )
```


#### Fecha hecho

A continuación, se analiza el comportamiento de la fecha hecho dad que es la fecha de interés. Primero se toma la serie mensual para separar tendencia, estacionalidad y residuales: 

```{r,fig.width=6, fig.height=3, warning=FALSE,message=FALSE}
serie_mensual <- base_datos %>% 
  mutate(ano_mes = floor_date(fec_hecho, "month")) %>% 
  count(ano_mes) %>%
  arrange(ano_mes)

# Convertir a objeto ts (suponiendo que comienza en enero de 2012)
ts_mensual <- ts(serie_mensual$n, 
                 start = c(year(min(serie_mensual$ano_mes)), 
                           month(min(serie_mensual$ano_mes))),
                 frequency = 12)

descomp_ts_mensual <- decompose(ts_mensual, type = "additive") 
plot(descomp_ts_mensual)
```

luego, se calcula la estacionalidad promedio por mes para ver en qué meses sube o baja regularmente esta serie:

```{r}
estacionalidad <- descomp_ts_mensual$seasonal
meses <- rep(1:12, length.out = length(estacionalidad))

# Agrupar y promediar
tendencia_estacional <- data.frame(
  mes = meses,
  valor = estacionalidad
) %>%
  group_by(mes) %>%
  summarise(efecto_promedio = mean(valor)) %>%
  mutate(mes_nombre = month.abb[mes])  # abreviatura del mes

tendencia_estacional %>% arrange(desc(efecto_promedio))

```

```{r}
aux <- base_datos %>%
  mutate(
    dia = day(fec_hecho),                      
    mes_num = month(fec_hecho),                 
    mes_nombre = month(fec_hecho, label = TRUE, abbr = FALSE)
  ) %>% count(mes_nombre, dia) %>% filter(mes_nombre == "enero", dia == 1) %>% pull(n)
```

El primero de enero es una día donde ocurren muchos casos de violencia a comparación del resto de día del año. Para este día se obtiene un total de `r aux` casos. Por este motivo, el siguiente gráfico se hace excluyendo este día. 

```{r}
base_datos %>%
  mutate(
    dia = day(fec_hecho),
    mes_num = month(fec_hecho),
    mes_nombre = month(fec_hecho, label = TRUE, abbr = FALSE)
  ) %>%
  filter((dia > 1 & mes_num == 1) | mes_num %in% c(3, 5, 8, 9)) %>%
  count(mes_nombre, dia) %>%
  ggplot(aes(x = dia, y = n, group = mes_nombre)) +
  geom_line(color = "#4B0082") +
  geom_point(color = "#4B0082", size = 1.5) +
  facet_wrap(~ mes_nombre, ncol = 2, scales = "free_y") +
  scale_x_continuous(breaks = seq(1, 31, by = 2)) +
  labs(
    title = "Distribución diaria de notificaciones por mes",
    x = "Día del mes",
    y = "Número total de casos"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(face = "bold",hjust = 0.5),
    strip.text = element_text(face = "bold"),
    panel.grid.minor = element_blank()
  )
```

#### Diferencia en días entre la fecha del hecho y fecha de notificación

```{r}
base_datos <- base_datos %>% mutate(dif_dias = ceiling(as.numeric(fec_not - fec_hecho)))
```

```{r}
summary(base_datos$dif_dias)
```
Debido a que la distribución presenta asimetría positiva, se opta por continuar el análisis considerando únicamente los registros con valores menores o iguales al percentil 80 (`r quantile(base_datos$dif_dias, probs = 0.8)`), con el fin de excluir la cola derecha

```{r}
data <- base_datos %>% filter(dif_dias <= 10)

grafico_barras(data,"dif_dias", facet_var = NULL,color_barras = "#AE86D3",top10 = FALSE) +
    labs(title = "Distribución de edad en años",x = "Edad (años)",y = "Número de casos (miles)")
```

```{r}
base_datos %>% group_by(anio = year(fec_hecho)) %>%
  summarise(promedio = ceiling(mean(dif_dias, na.rm = TRUE))) %>%
  ggplot(aes(x = anio, y = promedio)) +
  geom_line(color = "#4B0082", linewidth = 1.2) +
  geom_point(color = "#4B0082", size = 2.2) +
  geom_label(aes(label = promedio), color = "#4B0082",size = 2.2) +
  scale_x_continuous(
    breaks = function(x)
      seq(floor(min(x, na.rm = TRUE)), ceiling(max(x, na.rm = TRUE)), 1)) +
  scale_y_continuous(
    breaks = function(x)
      seq(floor(min(x, na.rm = TRUE)), ceiling(max(x, na.rm = TRUE)), 1)) +
  labs(title = "Promedio de diferencia (en días) por año del hecho", x = "Año del hecho", y = "Días promedio") + theme_minimal() +
  theme(
    strip.text = element_text(face = "bold"),
    plot.title = element_text(face = "bold",hjust = 0.5),
    axis.text.x = element_text(angle = 90,vjust = 0.5,hjust = 1),
    panel.grid.minor = element_blank())
```

### Pertenencia a grupo poblacional especial
```{r}
summary(base_datos[c("gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela",  
             "gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", 
             "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros")])
```

### Modalidad de violencia

```{r}
# df %>%
#   mutate(month = floor_date(date, "month")) %>%  # agrupar por mes
#   count(month, category1) %>%
#   ggplot(aes(x = month, y = n, color = category1)) +
#   geom_line() +
#   labs(title = "Evolución mensual por categoría", y = "Frecuencia")

```

```{r}
base_datos %>% count(def_naturaleza) %>%
  arrange(desc(n))
```

## Análisis multivariado

### Pruebas de asociación

A continuación, se seleccionan 4 variables principales y un conjunto de variables ("predictores") para cada una, con el fin de encontrar posibles relaciones. Para ello, se aplican pruebas no paramétricas dependiendo del tipo de variables. 

Para los predictores cualitativos, se construyen tablas de contingencia y, dependiendo de las frecuencias esperadas, se aplica una prueba de Chi-cuadrado o en caso de valores bajos, la prueba exacta de Fisher. Pero si la tabla es demasiado grande para el cálculo exacto, se recurre a una versión simulada de Fisher basada en Monte Carlo. 

En cambio, para predictores cuantitativos se utiliza la correlación de Spearman, ya que no requiere supuestos de normalidad y permite detectar relaciones monotónicas.

Finalmente, los resultados de cada análisis se organizan en una tabla que muestra la variable evaluada, la prueba realizada, los valores estadísticos y de significancia, junto con una interpretación clara sobre si existe o no evidencia estadística de relación con la variable de respuesta.

```{r}
test_cualit <- function(df, var_resp, var_pred) {
  tab <- table(df[[var_resp]], df[[var_pred]])
  
  if (any(chisq.test(tab)$expected < 5)) {
    test <- tryCatch(
      fisher.test(tab, simulate.p.value = TRUE, B = 1e5),
      error = function(e) fisher.test(tab, simulate.p.value = TRUE, B = 5000)
    )
    cramerv <- DescTools::CramerV(tab, bias.correct = TRUE)
    tibble(variable = var_pred,
           prueba = "Fisher (simulación)",
           estadistico = NA,
           p.value = round(test$p.value,3),
           cramersV = round(cramerv,3),
           decision = ifelse(test$p.value < alpha,
                             "Posible asociación",
                             "No asociación"))
  } else {
    test <- chisq.test(tab, correct = FALSE)
    cramerv <- DescTools::CramerV(tab, bias.correct = TRUE)
    tibble(variable = var_pred,
           prueba = "Chi-cuadrado",
           estadistico = round(unname(test$statistic),3),
           p.value = round(test$p.value,3),
           cramersV = round(cramerv,3),
           decision = ifelse(test$p.value < alpha,
                             "Posible asociación",
                             "No asociación"))
  }
}

# Spearman
test_cuant <- function(df, var_resp, var_pred) {
  y <- as.numeric(as.factor(df[[var_resp]]))
  x <- df[[var_pred]]
  
  test <- cor.test(x, y, method = "spearman", exact = FALSE)
  decision <- ifelse(test$p.value < alpha,
                     "Posible asociación",
                     "No hay evidencia estadística suficiente de asociación")
  
  tibble(variable = var_pred,
         prueba = "Spearman",
         estadistico = round(unname(test$statistic),3),
         p.value = round(test$p.value,3),
         decision = decision)
}

alpha <- 0.05
```

#### Hospitalización
```{r, warning=FALSE,message=FALSE}
base_datos <- base_datos %>%
  mutate(dif_dias_con = as.numeric(fec_con - fec_hecho))

respuesta <- "pac_hos" 
pred_cualit <- c("per_etn", "sexo_agre", "orient_sex","consum_spa","antec","def_naturaleza","sust_vict","mujer_cabf","tip_ss","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela",  
             "gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", 
             "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros")
pred_cuant  <- c("edad_")
pruebas_hosp <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_hosp %>% arrange(desc(cramersV)))
```

Sin evidencia estadística suficiente para asumir relación: gp_carcela y gp_desmovi

```{r}
prop.table(table(base_datos$def_naturaleza,base_datos$pac_hos),1)*100
```


#### Condición final
```{r, warning=FALSE,message=FALSE}
respuesta <- "con_fin" 
pred_cualit <- c("per_etn", "sexo_agre", "orient_sex","consum_spa","antec","def_naturaleza","sust_vict","mujer_cabf","tip_ss","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela",  
             "gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", 
             "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros")
pred_cuant  <- c("edad_")
pruebas_con_fin <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_con_fin %>% arrange(desc(cramersV)))
```
Sin evidencia estadística suficiente para asumir relación: sexo, ident_gene, consum_spa, sem_ges, gp_desplaz, gp_migrant,gp_carcela,gp_indigen,gp_pobicfb,gp_mad_com,gp_desmovi,gp_psiquia,gp_otros

#### Antecedente de violencia
```{r, warning=FALSE,message=FALSE}
respuesta <- "antec" 
pred_cualit <- c("sexo_agre", "orient_sex","consum_spa","def_naturaleza","mujer_cabf","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela",  
             "gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros","zona_conf","conv_agre","ambito_lug","cod_dpto_r","escenario","ac_mental","inf_aut")

pred_cuant  <- c("edad_")


pruebas_antec <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_antec %>% arrange(desc(cramersV)))
```
Sin evidencia estadística suficiente para asumir relación: ident_gene

#### Atención en salud mental
```{r, warning=FALSE,message=FALSE}
respuesta <- "ac_mental" 
pred_cualit <- c("per_etn", "sexo_agre", "orient_sex","consum_spa","def_naturaleza","mujer_cabf","tip_ss","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela","gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros","pac_hos","conv_agre","cod_dpto_n","tip_ss","zona_conf")
pred_cuant  <- c("edad_")
pruebas_mental <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_mental %>% arrange(desc(cramersV)))
```
Sin evidencia estadística suficiente para asumir relación: ident_gene y gp_desmovi


```{r}
prop.table(table(base_datos$sexo_agre,base_datos$ac_mental),1)*100
```


```{r}
prop.table(table(base_datos$pac_hos,base_datos$ac_mental),1)*100
```

```{r}
prop.table(table(base_datos$cod_dpto_n,base_datos$ac_mental),1)*100
```

#### Remisión a protección
```{r, warning=FALSE,message=FALSE}
respuesta <- "remit_prot" 
pred_cualit <- c("per_etn", "sexo_agre", "orient_sex","consum_spa","def_naturaleza","mujer_cabf","tip_ss","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela","gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros","pac_hos","conv_agre","cod_dpto_n","tip_ss","zona_conf","inf_aut","antec","area","sust_vict","per_etn","consum_spa","zona_conf")
pred_cuant  <- c("edad_")
pruebas_remit <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_remit %>% arrange(desc(cramersV)))
```

```{r}
prop.table(table(base_datos$def_naturaleza,base_datos$remit_prot),1)*100
```

```{r}
prop.table(table(base_datos$cod_dpto_n,base_datos$remit_prot),1)*100
```

#### Informe a autoridades competentes
```{r, warning=FALSE,message=FALSE}
respuesta <- "inf_aut" 
pred_cualit <- c("per_etn", "sexo_agre", "orient_sex","consum_spa","def_naturaleza","mujer_cabf","tip_ss","gp_discapa", "gp_desplaz", "gp_migrant", "gp_carcela","gp_gestan", "gp_indigen", "gp_pobicfb", "gp_mad_com", "gp_desmovi", "gp_psiquia", "gp_vic_vio","gp_otros","pac_hos","conv_agre","cod_dpto_n","tip_ss","zona_conf","inf_aut","antec","area","sust_vict","per_etn","consum_spa","zona_conf")
pred_cuant  <- c("edad_")
pruebas_inf_aut <- bind_rows(map_dfr(pred_cualit, ~test_cualit(base_datos, respuesta, .x)),
                          map_dfr(pred_cuant,  ~test_cuant(base_datos, respuesta, .x)))
knitr::kable(pruebas_inf_aut %>% arrange(desc(cramersV)))
```


```{r}
prop.table(table(base_datos$def_naturaleza,base_datos$inf_aut),1)*100
```

```{r}
prop.table(table(base_datos$cod_dpto_n,base_datos$inf_aut),1)*100
```


```{r}
prop.table(table(base_datos$sexo_agre,base_datos$inf_aut),1)*100
```

```{r}
prop.table(table(base_datos$area,base_datos$inf_aut),1)*100
```

### Tipo de seguridad social

```{r}
prop.table(table(base_datos$tip_ss,base_datos$ac_mental),1)*100
```
La mayoría de las víctimas afiliadas a regímenes especiales reciben atención en salud mental (65%), mientras que las víctimas en estado de afiliación indeterminado solo 13% la recibe.

### Tipo de violencia
```{r}
base_datos %>% 
  count(def_naturaleza) %>% 
  mutate(porc = round(100 * n / sum(n), 1)) %>% 
  arrange(desc(n))
```

```{r}
prop.table(table(base_datos$def_naturaleza,base_datos$ac_mental),1)*100
```
Más de la mitad de los casos de violencia contra mujeres corresponden a violencia física (51%)

```{r}
aux <- base_datos %>% mutate(def_naturaleza = case_when(
        def_naturaleza == "1" ~ "Física",
        def_naturaleza == "2" ~ "Psicológica",
        def_naturaleza == "3" ~ "Negligencia",
        def_naturaleza == "4" ~ "Sexual",
        def_naturaleza == "5" ~ "Sexual",
        def_naturaleza == "6" ~ "Sexual",
        def_naturaleza == "7" ~ "Sexual",
        def_naturaleza == "10" ~ "Sexual",
        def_naturaleza == "12" ~ "Sexual",
        def_naturaleza == "14" ~ "Sexual",
        def_naturaleza == "15" ~ "Sexual",
        is.na(def_naturaleza) ~ "Sin información",
        TRUE ~ def_naturaleza))
```

```{r}
aux %>%
  group_by(def_naturaleza, ac_mental) %>%
  summarise(n = n(), .groups = "drop") %>%
  group_by(def_naturaleza) %>%
  mutate(prop = n / sum(n)) %>%
  ggplot(aes(x = def_naturaleza, y = prop, fill = ac_mental)) +
  geom_bar(stat = "identity", position = "fill", alpha = 0.85) +
  geom_text(aes(label = scales::percent(prop, accuracy = 0.1)), 
            position = position_fill(vjust = 0.5), size = 3.5, color = "#4B0082",fontface = "bold") +
  labs(
    x = "Tipo de violencia",
    y = "Proporción (%)",
    fill = "Atención en salud mental"
  ) +
  scale_fill_manual(values = c("#90A9C3", "#AE86D3")) +
  theme_minimal() +
  scale_y_continuous(labels = scales::percent_format()) +
  theme(legend.position = "top")
```

### Escenario

```{r}
base_datos %>% 
  count(escenario) %>% 
  mutate(porc = round(100 * n / sum(n), 1)) %>% 
  arrange(desc(n))
```

```{r}
base_datos %>% 
  count(escenario) %>% 
  mutate(porc = round(100 * n / sum(n), 1)) %>% 
  arrange(desc(n))
```

### Departamento de notificación
```{r}
prop.table(table(base_datos$departamento_ocurrencia,base_datos$remit_prot),1)*100
```

```{r}
prop.table(table(base_datos$departamento_ocurrencia,base_datos$ac_mental),1)*100
```


### Informe a las autoridades
```{r}
prop.table(table(base_datos$sexo_agre,base_datos$inf_aut),1)*100
```

### Diagnósticos
```{r}
base_datos %>% filter(cbmte != "No aplica" & !is.na(cbmte)) %>%  
  count(cbmte, sort = TRUE) %>%         # cuenta y ordena
  mutate(porc = round(100 * n / sum(n), 3)) %>%  # añade porcentaje
  slice_max(order_by = n, n = 5)
```
CHOQUE HIPOVOLEMICO,PARO CARDIACO,VIOLENCIA FISICA,DESNUTRICION PROTEICOCALORICA SEVERA, SEPSIS, MUERTE SIN ASISTENCIA


